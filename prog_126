n=int(input())   # количество строк
ml=[]      # первый список, в который вносим все строки
zapros=[]   # вотрой список, в который вносим все запросы
for i in range(n):   # цикл, с помощью которого заполняем первый список
    s=input()
    ml.append(s)
k=int(input())    # количество запросов 2 списка
for i in range(k):  # цикл, который заполняет второй список
    x=input()
    x=x.lower()
    zapros.append(x) # запрос сохраняем в список нижним регистором ля удобства сравнения
for i in range (n): # цикл, который задает а как каждый по очереди элемент первого списка
    a=ml[i]
    a_low=a.lower() # задаем в элементе а все строчные буквы
    counter=0  #  будем подсчитывать сколько элементов мы сравнили
    for j in range(k):  #  выбираем для сравнения каждый элемент из второго списка по почереди длля сравнения с а
        b=zapros[j]
        if b in a_low:  # если данный элемент (строка) запроса есть в строке первого списка, то счетчик +1
            counter+=1
        if counter==k: # если все элементы запроса были внутри строки из первого списка, то счетчик будет равен длине второго списка 
            print(a)   # в таком случае выводим на печать данную строку
